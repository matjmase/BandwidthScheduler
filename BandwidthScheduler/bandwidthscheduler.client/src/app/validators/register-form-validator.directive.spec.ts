import { RegisterFormValidatorDirective } from './register-form-validator.directive';

describe('RegisterFormValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new RegisterFormValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
