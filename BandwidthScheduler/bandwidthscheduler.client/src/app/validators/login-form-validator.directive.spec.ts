import { LoginFormValidatorDirective } from './login-form-validator.directive';

describe('LoginFormValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginFormValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
