import { DateTimeRangeSelectorValidatorDirective } from './date-time-range-selector-validator.directive';

describe('DateTimeRangeSelectorValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new DateTimeRangeSelectorValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
