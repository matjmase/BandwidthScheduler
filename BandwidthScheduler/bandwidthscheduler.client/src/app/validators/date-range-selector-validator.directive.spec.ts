import { DateRangeSelectorValidatorDirective } from './date-range-selector-validator.directive';

describe('DateRangeSelectorValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new DateRangeSelectorValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
